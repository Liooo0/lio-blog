---
title: "房租专家 — 房租水电收据管理"
description: "给房东用的桌面收据管理软件：A4 PDF / 针式打印一式两联、月度统计、CSV 导出"
date: 2026-08-04
tech: ["Python", "ttkbootstrap", "SQLite", "reportlab"]
status: "in-progress"
---

## 为什么做这个

家里出租房子，每个月要开收据。传统方式是针式打印机加复写纸，一式两联，底联留着、租客拿走一联。数据全靠手写和本子记，查历史、算月度汇总都很痛苦。我需要一个工具：录入快、能打印、能统计、能导出。

## 功能

- **房间管理** — 每个房间预设房租、水单价、电单价
- **新建收据** — 选房间自动带出单价 → 填本次水/电表读数 → 自动算金额 → 保存；支持"从上次读数复制"和批量录入
- **A4 PDF 打印** — reportlab 生成简洁 A4 版式，含中文大写金额，可预览可送打印
- **针式打印模式** — 传统针式打印机只认纯文本点阵，所以这个模式生成等宽字体纯文本收据（约 40-80 字符宽），走系统 `lp` 命令送打印机，也可存成 .txt 拷到 Windows 打；配复写纸一次打两联
- **月度汇总** — 按年份选月份，房租/水电合计表格 + 柱状图
- **数据导出** — CSV / Excel；数据库一键备份，退出时自动备份

## 这一夜修掉了 5 个致命 bug

这个工具之前是个"README 很好看、点进去全报错"的半成品。一夜之间定位并修复了 5 个让软件基本没法用的问题：

| Bug | 表现 |
|---|---|
| 编辑保存不回写 | 改了等于没改 |
| 改动丢失 | 操作完数据还是旧的 |
| 保存后不刷新 | 列表和月度汇总不更新，像没保存成功 |
| 默认按 2026 筛选 | 真实数据在 2024 年，打开像"数据全丢了" |
| 环境 Tk 缺失 | macOS 上根本启动不起来 |

修完又做了一轮 UI 优化：主题实时切换（不用重启）、费用项可定制裁剪、新建收据的单价继承逻辑修正。

## 实际效果

- README 承诺的六大功能 macOS 全部实测可用
- **36 项自动化测试全绿**（窗口实例化、CRUD、算费、导出、针式排版对齐）
- 真实数据（3 房 3 收据）完好无损，迁移前先备份
- 日常用法：双击 `启动房租专家.command` 就能跑

还在持续迭代，所以状态是进行中。
